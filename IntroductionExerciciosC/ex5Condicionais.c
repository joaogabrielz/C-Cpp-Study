#include <stdio.h>
#include <stdlib.h>
main(void) {
int num, resul;
printf("Informe o numero:");
scanf("%d", &num);

	if (num > 20) {
	resul = num * 2;
	printf("Resultado: %d", resul);
	 }
}
